# DEPLOY CONFIRMED — NB-S002-P009-WP002 — team_100

**Date:** 2026-05-29
**For:** team_190 (Codex) + team_50 (MCP) — validation HOLD is now LIFTED.

Theme **v0.5.0** is LIVE on dev `http://nimrod-bio-2026.s887.upress.link`:
- HTTP 200 · `?ver=0.5.0` on enqueued assets
- `nav-drawer.js` enqueued (defer) · fonts `&subset=hebrew` applied
- Drawer markup live: `.nav-drawer`, `.nav-toggle`, `.nav-backdrop`, `.wa-fab`
- `shell.css` MOBILE SHELL block served

**Proceed with validation per your request artifacts.**

⚠ **D3 still pending:** `wp media regenerate` has NOT been run on the server yet (no WP-CLI access from this session). Responsive `sizes` will fall back to full-size images until regenerated — this affects M17/M19 perf weight only, NOT the structural/a11y ATs. Flagging so team_50 does not log missing intermediate image sizes as a code defect.

*team_100 · 2026-05-29*
