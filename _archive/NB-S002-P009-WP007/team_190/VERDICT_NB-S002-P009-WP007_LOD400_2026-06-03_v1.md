---
type: VERDICT
document_title: "VERDICT — NB-S002-P009-WP007 — LOD400 full design implementation — L-GATE_SPEC"
document_version: "v1"
document_date: "2026-06-03"
date: 2026-06-03
team_id: team_190
phase_owner: team_190
project: nimrod-bio
milestone: V200
program: P009
work_package: NB-S002-P009-WP007
gate: L-GATE_SPEC
track: A
effort: NORMAL
builder: team_100
architect: team_100
validator: team_190
builder_engine: "Claude Code / team_100"
validator_engine: "Cursor / Composer (team_190)"
cross_engine: preserved (Iron Rule #1)
spec_ref: "_aos/work_packages/NB-S002-P009-WP007/LOD400_NB-S002-P009-WP007.md"
validate_request_ref: "_COMMUNICATION/team_190/VALIDATE_REQUEST_NB-S002-P009-WP007_LOD400_2026-06-03_v1.md"
design_ssot_ref: "_COMMUNICATION/team_35/HANDOFF_v5_2026-06-03/Precision_Mockup_v5.html"
baseline_commit: "161e8078"
baseline_theme_version: "v0.7.19"
verdict: PASS_WITH_FINDINGS
route_recommendation: "PASS_WITH_FINDINGS -> team_100 issues implementation-session HANDOFF (LOD400 §9); optional AT/G3a-path cleanup at HANDOFF; build -> deploy -> L-GATE_VALIDATE"
---

# VERDICT — NB-S002-P009-WP007 — LOD400 (L-GATE_SPEC) — v1

## 1. Executive summary

**Gate result: `PASS_WITH_FINDINGS`**

Team_190 performed independent **spec-level** cross-engine review of `LOD400_NB-S002-P009-WP007.md` against `Precision_Mockup_v5.html`, the team_35 design-completeness reply (rows 1–5), and repo baseline @ `161e8078` (theme v0.7.19). This gate validates **LOD400 completeness and checkability** before it becomes the build contract — **nothing is built or deployed under this verdict**.

**Confirmed:** every page-type not in the v4 precision pass is IN-scope with a theme template + v5 screen + AT row (AT-1..AT-7). G1 projects archive is correctly **EXCLUDED** (already DONE in WP001). Global acceptance (AC-A..AC-G), super-locks, module-CSS-only discipline, phasing (§6), validation routing (§8), and handoff plan (§9) are unambiguous and probe-backed.

**Non-blocking advisories:** several §4 AT rows drift from v5 numeric/layout values and selector vocabulary (F1); G3a override source path in LOD400 §2 is broken and the block is absent from HANDOFF `t1.css` — builder must lift from v5 mockup L896–913 (F2). §4 method already names v5 as the pixel-diff authority, so build may proceed with mockup override where AT text conflicts.

**Route:** team_100 issues the implementation-session HANDOFF per LOD400 §9, then build → deploy → qa_probe + a11y non-regression → separate **L-GATE_VALIDATE** (team_190 + team_50).

---

## 2. Cross-engine attestation

| Role | Team | Engine | Artifact |
|---|---|---|---|
| Design SSoT | team_35 | Claude Design | `_COMMUNICATION/team_35/HANDOFF_v5_2026-06-03/Precision_Mockup_v5.html` |
| LOD400 author | team_100 | Claude Code | `_aos/work_packages/NB-S002-P009-WP007/LOD400_NB-S002-P009-WP007.md` |
| Spec validator | team_190 | Cursor / Composer | this verdict |

Iron Rule #1 maintained: validator engine (Cursor) ≠ author engine (Claude Code).

---

## 3. Per-check result table (VALIDATE_REQUEST §"What to validate")

| # | Check | Verdict | evidence-by-path | route_recommendation |
|---|---|---|---|---|
| **1** | **Coverage** — every non-v4 page-type mapped to template + v5 screen; nothing required missing | **PASS** | LOD400 §3 IN: G2 (T2 index `archive-service.php`→`t2`, T2 single `single-service.php`→`t2s`, T3 `single-project.php`→`t3`, T4 `single.php`→`t4`, T5 `home.php`→`t5`) + G3 (know/code `page-know.php`/`page-code.php`→`t1` switcher, heritage `page-heritage.php`→`heritage`). All 8 templates exist under `nimrod.bio/wp-content/themes/nimrod-bio-2026/`. v4-done pages OUT in §1/§3. team_35 reply rows 1–5 confirm v5 authority. | Proceed — scope complete. |
| **2** | **SSoT / AT fidelity** — AT-1..AT-7 reflect v5 screens (blocks/classes/precision values) | **PASS_WITH_FINDINGS** | Block structure matches v5 for all 7 screens (spot-check: `Precision_Mockup_v5.html` screens `t2`,`t2s`,`t3`,`t4`,`t5`,`t1`,`heritage`). **Drift:** AT-1 grid `repeat(4,1fr)` vs v5 `.svc-grid` `repeat(2,1fr)` L739; AT-1 h1 `clamp(34→60)` vs v5 `.page-hero h1` `clamp(40px,6.2vw,84px)` L600; AT-2 hero `1fr 1fr` vs v5 `1.2fr .85fr` L925, feat grid 2×2 not 4-col L933; AT-3 h1 `clamp(28→44)` vs v5 `.t3-hero-title` `clamp(34px,5vw,64px)` L822; AT-4 numbered h2 `.num` — absent on T4 (heritage-only, AT-7); AT-6 selectors mix live-theme (`.anchor-card`, `.post-card`) vs v5 (`.lat-anchor`, `.post-row`) L896–913. AT-5, AT-7 strong match. See §4 F1. | PASS — amend ATs or add “v5 wins” rule at HANDOFF; mockup is authoritative for pixel diff. |
| **3** | **STALE-GAP G1** — projects archive DONE; correctly EXCLUDED | **PASS** | `nimrod.bio/wp-content/themes/nimrod-bio-2026/inc/cpt-project.php` L28: `'has_archive' => 'projects'`. `archive-project.php` present (G1/WP001). HTTP `/projects/` → **200** local (`localhost:8085`) + dev (`nimrod-bio-2026.s887.upress.link`). WP006 axe: `projects-archive` 0 violations (`docs/qa/cdp/v200b/team190/axe_result.json`). team_35 reply L54–55 (“still missing”) is **stale**. | Proceed — non-regression only per AC-F. |
| **4** | **a11y non-regression (AC-A)** — WP006 baseline; know/code deep-on-wash flagged | **PASS** | AC-A: axe 0 serious/critical + 0 color-contrast; Lighthouse a11y ≥95 on all affected routes. AT-6: explicit AA re-check for `--w-*-deep` on `--know-wash`/`--code-wash` after G3a. WP006 baseline: `docs/qa/cdp/v200b/team190/axe_result.json` `totalSerious: 0` (15 pages). Harness: `scripts/qa/cdp/axe_probe.mjs`, `scripts/qa/cdp/qa_probe.mjs`, `docs/QA_HARNESS.md`. Pre-WP006 `a11y_baseline/axe_result.json` (78 contrast nodes) is not the regression target. | Proceed — re-run axe + LH after each phase per §6. |
| **5** | **Constraints** — module CSS/template-parts only; locks; G3a in-WP | **PASS** | §1/§4/AC-D: module CSS + template-parts only; 0 inline; no overrides; `system.css` byte-identical (`git diff` empty). §7 super-locks + AC-C CDP `--absent` + grep. RTL logical properties in §4 method + team_35 reply. G3a lands in Phase 1 (§3, AT-6, team_35 row 5) — not hot-patch. AC-E byte-parity + FTPS. | Proceed — enforce via diff audit at L-GATE_VALIDATE. |
| **6** | **Phasing / routing / handoff** — §6, §8, §9 sound | **PASS** | §6: Phase 1 G3a+world → Phase 2 G2 singles → Phase 3 indexes/post; deploy+qa between phases. §8: L-GATE_SPEC (this) → L-GATE_VALIDATE post-build (team_190 + team_50). §9 HANDOFF checklist sufficient (LOD400, v5 path, G3a, ATs, WP006 baseline, FTPS, baseline pin). **Observational:** `roadmap.yaml` WP007 shows `lod_status: LOD300`, `assigned_builder: team_35` — team_100 should reconcile on PASS (not a LOD400 defect). | Proceed — team_100 issues §9 HANDOFF. |

**Aggregate: 5/6 PASS, 1/6 PASS_WITH_FINDINGS → gate `PASS_WITH_FINDINGS`.**

---

## 4. Findings

| id | severity | evidence-by-path | route_recommendation |
|---|---|---|---|
| T190-P009-WP007-SPEC-F1 | INFO | AT numeric/selector drift vs v5: AT-1 `.svc-grid` 2-col not 4-col (`Precision_Mockup_v5.html` L739); AT-1 h1/lede clamps (L600); AT-2 hero/feats (L925–933); AT-3 h1 (L822); AT-4 numbered h2 on T4 absent (L776 vs heritage L963–964); AT-6 selector vocabulary (L896–913 vs LOD400 §4 AT-6 prose). | PASS — add explicit “v5 mockup wins on conflict” at HANDOFF or patch AT rows before build; non-blocking for spec gate. |
| T190-P009-WP007-SPEC-F2 | INFO | G3a +25-line block: LOD400 §2 cites `04_build_layer/HANDOFF_CLAUDE_CODE_V200/` (path absent). `G2_G3_C2_PRECISION_2026-06-03_v1.md` claims append to HANDOFF `t1.css` but repo grep: **no** `.t1-world-know` in HANDOFF theme. Canonical block = v5 mockup L896–913 (design twin per team_35 row 5). Live `nimrod.bio/.../t1.css`: no G3a yet (expected — lands in WP007 Phase 1). | PASS — pin G3a source to v5 L896–913 in HANDOFF; builder copies into live `t1.css` Phase 1. |
| T190-P009-WP007-SPEC-F3 | INFO | Stale downstream doc: `DESIGN_COMPLETENESS_REPLY_team_100_2026-06-03_v1.md` L54–55 still lists G1 projects archive as “still to build”; contradicts WP001 + repo. LOD400 §1/§3 correctly excludes G1. | PASS — no LOD400 change required; note for team_100 HANDOFF context. |

---

## 5. AT spot-check detail (Check 2 supplement)

| AT | v5 screen | Structure match | Precision drift |
|---|---|---|---|
| AT-1 | `t2` | ✓ crumbs → page-hero → svc-grid cards → bridges-band → final-cta | Grid 2-col; h1/lede clamps; breakpoint 760px not 520px |
| AT-2 | `t2s` | ✓ svc-single-hero → feat-grid → svc-pull → svc-steps → projects-row → bridge → CTA | Hero ratio; 2×2 feat grid; breakpoint 820px not 1000px |
| AT-3 | `t3` | ✓ t3-hero-grid → about-prose → outcomes → linked-services → proj-gallery → related → CTA | h1 clamp values |
| AT-4 | `t4` | ✓ article-shell.has-aside → post-layout → article-foot | No numbered h2; use `.post-hero-meta-top` not “meta-top” |
| AT-5 | `t5` | ✓ blog-header-grid → blog-toolbar → posts-grid feat span → blog-end | Minor: `.fb.is-on` vs `.blog-filter` tablist — functional OK |
| AT-6 | `t1` switcher | ✓ `.t1-world-know`/`.t1-world-code` wash + accent remap | Selector names differ live vs mockup; G3a source = v5 L896–913 |
| AT-7 | `heritage` | ✓ heritage-hero → heritage-reading/body → heritage-end | Strong match |

---

## 6. Route recommendation

**`PASS_WITH_FINDINGS` → team_100**

1. Issue implementation-session **HANDOFF** per LOD400 §9 (include F1/F2 advisories: v5 wins on AT conflict; G3a from mockup L896–913).
2. Optionally patch LOD400 §4 AT rows and §2 G3a path before or within HANDOFF — not required to open build.
3. Execute phased build (§6) → deploy → `qa_probe.mjs` + `axe_probe.mjs` non-regression after each phase.
4. File separate **L-GATE_VALIDATE** request on deployed result to team_190 + team_50 (§8).

**Roadmap (Iron Rule #4 — team_100 single writer):** append WP007 `gate_history` entry with `result: PASS_WITH_FINDINGS`, update `lod_status: LOD400`, advance gate toward implementation/L-GATE_VALIDATE. team_190 does **not** edit `_aos/roadmap.yaml`.

Suggested `gate_history` append (for team_100):

```yaml
- gate: L-GATE_SPEC
  result: PASS_WITH_FINDINGS
  date: "2026-06-03"
  notes: "team_190 Cursor cross-engine spec review. VERDICT_NB-S002-P009-WP007_LOD400_2026-06-03_v1.md. Findings: AT numeric/selector drift vs v5 (F1); G3a source path -> v5 mockup L896-913 (F2). G1 exclusion confirmed stale. Route: HANDOFF to implementation session."
```

---

*team_190 | L-GATE_SPEC | NB-S002-P009-WP007 LOD400 | 2026-06-03 | cross-engine preserved | spec review only — no build*
