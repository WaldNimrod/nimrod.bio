---
type: LOD400_SPEC
wp_id: NB-S002-P003-WP001
project: nimrod-bio
milestone: V200
program: P003
label: "T7 Home template"
track: A · STANDARD
author: team_100 (nimrodbio_arch)
date: 2026-05-25
program_ref: _aos/work_packages/S002/P003/LOD300_P003_program.md
estimated_effort: 3 working days
---

# LOD400 — NB-S002-P003-WP001 — T7 Home

**📖 קרא קודם:** `LOD300_P003_program.md` — דפוסים משותפים. ה-LOD400 הזה ממוקד ב-T7-specific deltas.

## 1. Mission

Implement WordPress `front-page.php` as a single-file template rendering the T7 Home page per design package, with hero variant **statement** and Unless placement **ribbon** (both locked by team_35).

## 2. Source files

- `sources/team_35_design_package/_handoff/templates/T7 Home.html` (HTML structure + JSX data)
- `sources/team_35_design_package/_handoff/templates/T7-styles.css` (472 lines — copy to `assets/css/t7.css` minus shell selectors)
- `sources/team_35_design_package/_handoff/templates/T7-data.jsx` (data shape reference)

## 3. Deliverables

```
nimrod-bio-2026/
├── front-page.php                       # NEW — T7 layout
├── assets/css/t7.css                    # NEW — extracted from T7-styles.css
└── inc/template-styles-t7.php           # NEW — conditional enqueue per program §4
```

Modified: bump `NB_THEME_VERSION` from `0.2.1` to `0.3.0` in `functions.php`.

## 4. T7 sections (in order)

Per design §6.T7. Hero variant = `statement` (locked). Unless placement = `ribbon` (locked).

1. **Hero — statement variant**
   - Big sentence: "פיזיקה, אקולוגיה, קוד וחקלאות — אותה מערכת. שלוש זרועות, 3× חיבורים."
   - SVG ER diagram (3 circles for worlds, 3 intersection dots — see T7-data.jsx ~line 100, render as inline `<svg>`)
   - Meta row: 3 facts (e.g., "4 חממות · ייעוץ", "1 חקלאות · קומון", "1 SFA · קוד")
2. **§01 העולמות** — 3 large cards in world colors. Each card links to `/world/{slug}/`. Use `nb_world_chip()` and CPT-derived service counts.
3. **§02 פרויקטים** — Grid: 1 large + 2 small. Pull 3 featured projects (configurable, see program §8). Render with `nb_stage_stamp()`.
4. **Unless ribbon** — Full-width black band between projects and posts. Text: "העולם הוא כזה — *אלא אם כן*." Spark color on "אלא אם כן".
5. **§03 מהבלוג** — 4 recent posts (square aspect). `WP_Query( ['posts_per_page' => 4, 'order' => 'DESC'] )`.
6. **Final CTA** — 2 paths: "צור קשר" (link `/contact/`) + "הצטרף ל-SFA" (link `/services/sfa/`).

## 5. PHP skeleton

```php
<?php
/**
 * front-page.php — T7 Home (statement / ribbon)
 */
defined( 'ABSPATH' ) || exit;
get_header();
?>

<section class="t7 t7-hero hero-statement">
    <div class="t7-wrap">
        <!-- HERO statement (text + SVG diagram + meta row) -->
        <!-- Verbatim copy from T7 Home.html lines 30-90 -->
    </div>
</section>

<section class="t7-section t7-worlds" aria-labelledby="t7-worlds-title">
    <div class="t7-wrap">
        <?php echo nb_sec_head( 1, 'העולמות', 'שלוש זרועות · שורש אחד', 'כל זרוע פעילה בפועל. הקישוריות ביניהן היא הייחוד.' ); ?>
        <div class="worlds-grid">
            <?php foreach ( [ 'soil', 'know', 'code' ] as $w ): ?>
                <a href="<?php echo esc_url( home_url( "/world/$w/" ) ); ?>" class="world-card world-<?php echo esc_attr( $w ); ?>">
                    <span class="world-name"><?php echo esc_html( nb_world_label( $w ) ); ?></span>
                    <?php
                    $count = nb_query_by_world( 'service', $w, -1 )->found_posts;
                    ?>
                    <span class="world-count"><?php echo (int) $count; ?> פעילויות</span>
                </a>
            <?php endforeach; ?>
        </div>
    </div>
</section>

<section class="t7-section t7-projects">
    <div class="t7-wrap">
        <?php echo nb_sec_head( 2, 'פרויקטים', 'בנו · מבנים · מחפשים', 'פרויקטים שמראים את הקישוריות בפעולה.' ); ?>
        <div class="projects-grid">
            <?php
            $featured = new WP_Query( [
                'post_type' => 'project', 'posts_per_page' => 3,
                'meta_query' => [
                    [ 'key' => '_nb_stage', 'value' => [ 'live', 'seeking-partners' ], 'compare' => 'IN' ],
                ],
            ] );
            while ( $featured->have_posts() ) : $featured->the_post();
                $stage = nb_meta( get_the_ID(), 'stage' );
                ?>
                <a class="proj-card" href="<?php the_permalink(); ?>">
                    <?php if ( has_post_thumbnail() ) the_post_thumbnail( 'large' ); ?>
                    <?php echo nb_stage_stamp( $stage ); ?>
                    <h3><?php the_title(); ?></h3>
                    <p><?php echo esc_html( nb_meta( get_the_ID(), 'summary' ) ); ?></p>
                </a>
            <?php endwhile;
            wp_reset_postdata();
            ?>
        </div>
    </div>
</section>

<aside class="unless-ribbon" role="complementary">
    <div class="t7-wrap">
        <p>העולם הוא כזה — <em>אלא אם כן</em>.</p>
    </div>
</aside>

<section class="t7-section t7-posts">
    <div class="t7-wrap">
        <?php echo nb_sec_head( 3, 'מהבלוג', 'מחשבות שיורדות לאדמה', '' ); ?>
        <div class="posts-grid posts-grid-4">
            <?php
            $recent = new WP_Query( [ 'post_type' => 'post', 'posts_per_page' => 4 ] );
            while ( $recent->have_posts() ) : $recent->the_post(); ?>
                <a class="post-card post-square" href="<?php the_permalink(); ?>">
                    <?php if ( has_post_thumbnail() ) the_post_thumbnail( 'medium' ); ?>
                    <h4><?php the_title(); ?></h4>
                </a>
            <?php endwhile;
            wp_reset_postdata();
            ?>
        </div>
    </div>
</section>

<section class="t7-section final-cta">
    <div class="t7-wrap">
        <h2>איך אפשר להתחיל?</h2>
        <div class="cta-paths">
            <a class="btn btn-primary" href="<?php echo esc_url( home_url( '/contact/' ) ); ?>">צור קשר</a>
            <a class="btn btn-spark" href="<?php echo esc_url( home_url( '/services/sfa/' ) ); ?>">הצטרף ל-SFA</a>
        </div>
    </div>
</section>

<?php get_footer(); ?>
```

## 6. `assets/css/t7.css` extraction

Copy `T7-styles.css` verbatim. The file has 472 lines. Skip any `.shell-*` selectors (they already in shell.css) — for T7 there are none, so it's a clean copy.

Add header comment at top:
```css
/* t7.css — locked from team_35 T7-styles.css. DO NOT EDIT body. */
```

## 7. `inc/template-styles-t7.php`

```php
<?php
defined( 'ABSPATH' ) || exit;

add_action( 'nb_enqueue_template_styles', function () {
    if ( is_front_page() ) {
        wp_enqueue_style( 'nb-t7', NB_THEME_URI . '/assets/css/t7.css', [ 'nb-shell' ], NB_THEME_VERSION );
    }
} );
```

## 8. Acceptance tests (T7-specific)

| # | Test | Pass criteria |
|---|---|---|
| H1 | `/` returns 200 + `front-page.php` rendered | `curl -I /` → 200; HTML contains `class="t7-hero hero-statement"` |
| H2 | Hero sentence verbatim | grep `"פיזיקה, אקולוגיה, קוד וחקלאות"` in HTML |
| H3 | ER diagram SVG inline | grep `<svg` with 3+ `<circle` elements |
| H4 | 3 world cards | grep `world-card world-soil`, `world-know`, `world-code` |
| H5 | 3 featured projects rendered | count `.proj-card` ≥ 1 (test data may have <3 — note in COMPLETION) |
| H6 | Unless ribbon | grep `<em>אלא אם כן</em>` |
| H7 | 4 recent posts | count `.post-card` matches available posts (up to 4) |
| H8 | Final CTA 2 buttons | grep `btn-primary` AND `btn-spark` |
| H9 | t7.css enqueued | view-source: `<link … id='nb-t7-css' …t7.css?ver=0.3.0>` |
| H10 | Baseline §11 (program) all PASS | Shell, footer, validate, RTL, helpers, git, version |

## 9. Out of scope (T7)

- BOOM A/B variant (T-01 — future Stage 5 work)
- Watercolor backgrounds (T-03)
- Mobile-specific hero layout (T-02)
- Logo (T-04)

## 10. Risks

| Risk | Mitigation |
|---|---|
| ER diagram SVG path complexity in HTML escapes badly | Use heredoc PHP syntax; verify rendered SVG validates |
| Featured-project query returns <3 records | Test data WP004 ensures ≥3; if not, T7 renders empty slots gracefully |
| Unless ribbon "אלא אם כן" loses Hebrew italic | `<em>` is text element; CSS handles styling |

## 11. Effort: 3 days

- D1: front-page.php structure + CSS extraction + enqueue
- D2: SVG integration + dynamic CPT queries + Unless ribbon
- D3: H1-H10 tests + bug fixing + COMPLETION

---

*LOD400 — T7 Home | NB-S002-P003-WP001 | nimrod-bio · V200*
