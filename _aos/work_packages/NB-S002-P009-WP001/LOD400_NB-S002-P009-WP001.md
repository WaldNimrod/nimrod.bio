# LOD400 — NB-S002-P009-WP001 — V200 UI Precision Package (2 new templates + 3-source fixes)

**Date:** 2026-06-03
**Author:** team_100
**WP:** NB-S002-P009-WP001
**Type:** LOD400 implementation spec
**Track:** A · **Effort:** NORMAL
**Baseline:** theme `nimrod-bio-2026` @ commit `a35a67df` (v0.7.15)
**Source handoff:** `_COMMUNICATION/team_35/HANDOFF_CLAUDE_CODE_V200_2026-06-03/` (README + 6 theme files + 2 render-parity previews)
**Validation:** external — team_50 (dev QA) + team_190 (constitutional L-GATE_VALIDATE), cross-engine (≠ builder)

---

## 1. Mission
Land team_35's V200 precision package onto the live theme **exactly and completely**: two new templates (home §06 recent-posts; `/projects/` archive) + every precision fix from the three input sources (team_00 comments · team_35 mockup walk · team_50 live artifacts). All edits in module CSS / template-parts / theme source — **NO inline styles, NO overrides layer** (binding team_00 rule). Result = full Precision-Mockup-v4 fidelity, deployed to dev, externally validated, ready for the V200 cutover gate.

## 2. Inputs (source of truth)
- **Handoff theme files** (drop-in, land on `a35a67df`): `front-page.php`, `archive-project.php` (NEW), `inc/cpt-project.php`, `inc/template-styles-t1.php`, `assets/css/t7.css`, `assets/css/t1.css`.
- **README master index:** `_COMMUNICATION/team_35/HANDOFF_CLAUDE_CODE_V200_2026-06-03/README.md`.
- **Render-parity previews:** `S06 Recent-Posts Preview.html`, `Projects Archive Preview.html`.
- **Design SSOT:** `Precision Mockup v4.html`. **Drift register:** `DRIFT_REGISTER_MOCKUP_V4_2026-06-01_v1.md`.
- **Procedures:** `docs/UPRESS_FTPS_MANDATORY_PROCEDURE.md` · `docs/QA_HARNESS.md`.
- **Locks:** `SITE_HANDOFF_2026-05-31_v1.md` §2 (Micha · demonstrate-never-name).

## 3. Change inventory (quantified diff vs repo @ a35a67df)
| File | Δ | Scope |
|------|---|-------|
| `front-page.php` | +70L | §06 recent-posts block (WP_Query post·publish·5·DESC → `.posts-grid` 1 `.rp-card.feat`+4 `.rp-card`, world chips); between §05 and manifesto |
| `archive-project.php` | NEW 75L | `/projects/` archive — all published projects newest-first, proj-card markup + `nb_stage_stamp()` + empty-state |
| `inc/cpt-project.php` | 2L | `has_archive => false` → `'projects'` |
| `inc/template-styles-t1.php` | 2L | enqueue `t1.css` on `is_post_type_archive('project')` |
| `assets/css/t7.css` | ~149L | `.posts-grid`/`.rp-card.feat` canonical block; retire 3 stale scaffolds (`.recent-posts`/`.posts-grid-4`/`.post-card.post-square`); bridge-title underline; Unless restructure |
| `assets/css/t1.css` | ~55L | `.projects-archive` block (3→2→1-up); lattice-crush mobile fix; bridge-title underline |

## 4. Phases — execution flow

### Phase 1 — Pre-flight (read-only)
1. Confirm repo HEAD == `a35a67df` lineage (or note delta); `git status` clean of theme edits.
2. Byte-compare each handoff `theme/<f>` against repo `nimrod.bio/wp-content/themes/nimrod-bio-2026/<f>` to re-confirm the Δ table (§3). Flag any unexpected divergence (repo moved under the handoff).
3. Lock-scan all 6 handoff files incl. comments/alt/aria → expect 0 (`CDIP·cross-domain·אנטרופיה·נגנטרופיה·רקורסיה·פרמקלצר·3×·אינסטנסים·קואופרטיב·קומון·Micha·מיכה`).

### Phase 2 — Land the theme files (one logical writer; Iron Rule #4)
1. Copy the 6 handoff `theme/` files into the live theme tree (overwrite the 5 existing, add `archive-project.php`).
2. Bump `NB_THEME_VERSION` `0.7.15` → `0.7.16` in `functions.php`.
3. `php -l` every changed/new PHP (`front-page.php`, `archive-project.php`, `cpt-project.php`, `template-styles-t1.php`, `functions.php`); CSS brace-balance check on `t7.css`, `t1.css`.

### Phase 3 — Deploy (per UPRESS_FTPS_MANDATORY_PROCEDURE)
1. FTPS preflight (IP allowlist incl. this machine; TCP 21).
2. Upload ONLY the changed files via `scripts/upress_ftps_upload.py --local-dir <minimal-mirror>` (stage relative paths; never whole tree).
3. **Flush permalinks** — Settings → Permalinks → Save (REST or wp-cli equiv) so the new `has_archive=>'projects'` resolves `/projects/`. (Mandatory — `/projects/` 404s until flushed.)
4. Re-download each changed file one-per-connection (`scripts/upress_ftps_download.py --dest <tmp> --files <relpath>`); confirm **byte-parity** repo == deployed. Re-sync repo + commit.

### Phase 4 — Build verification (team_100, browser-real)
1. `node scripts/qa/cdp/qa_probe.mjs` over all pages + `/projects/`:
   - **§06 present on `/`** — `.posts-grid` with 1 feat + ≤4 cards, real post titles + world chips.
   - **`/projects/` HTTP 200** — archive renders real projects (scope chip + stage stamp + world meta), empty-state path sane.
   - **T1 lattice intact at 375/900/1440** — no crushed anchor (B1 fix); `.lat-anchor` spans full width on mobile.
   - **Bridge titles underlined, body clean** (A2); **Unless stacked** English-large / Hebrew-small (A1).
   - **0 horizontal overflow** at 375/1440 on every page; **lock-scan 0**.
2. Δ1 line-breaks: no forced `<br>` mid-measure; Δ2 world-card images uniform `16/10` equal heights — visually confirm via screenshots.
3. Compare live §06 + `/projects/` against the two render-parity preview HTMLs.

### Phase 5 — External validation (cross-engine)
1. **team_50** dev QA pass (independent): per-page matrix + screenshots + Lighthouse + lock-scan → `QA_REPORT`.
2. **team_190** constitutional L-GATE_VALIDATE (immutable, builder≠validator) → VERDICT.
3. team_100 triages findings → fix-cycle to team_35 if any, else advance.

## 5. Deliverables
- 6 theme files landed + deployed (v0.7.16), repo byte-synced + committed.
- `/projects/` resolving; §06 live on home.
- team_100 build-verification record; team_50 QA report; team_190 VERDICT.
- Updated `DRIFT_REGISTER` + roadmap gate note.

## 6. Acceptance criteria (checkable on dev)
- [ ] §06 recent-posts renders on `/` with ≥1 real post (feat) + up to 4 more, world chips from each post's `world` terms; section sits between §05 and manifesto.
- [ ] `/projects/` returns 200, lists every published `project` newest-first as proj-cards (scope+stage+world), empty-state when none.
- [ ] `cpt-project.php` `has_archive => 'projects'`; permalinks flushed; home §05 + T1 "לכל הפרויקטים" links resolve (no 404).
- [ ] `t1.css` enqueued on project archive (via `template-styles-t1.php`).
- [ ] T1 lattice: at ≤900px no phantom `grid-column:3/4` column; anchor full-width, not crushed.
- [ ] Bridge-card: underline on `h3` title only; body no underline (t7 + t1 parity).
- [ ] Unless lockup: English source large, Hebrew translation small/muted beneath (stacked).
- [ ] Δ1: no awkward forced line-breaks; Δ2: world-card media uniform 16/10, equal heights.
- [ ] 3 stale CSS scaffolds retired (`.recent-posts`/`.posts-grid-4`/`.post-card.post-square`); no orphan rules reintroduced.
- [ ] CDP: 0 horizontal overflow @375/1440 all pages; **lock-scan 0** (incl. alt/aria/comments).
- [ ] Repo == deployed byte-parity; `NB_THEME_VERSION` bumped; theme edits committed (no inline/no overrides layer).
- [ ] team_190 verdict = PASS / PASS_WITH_DEFERRALS.

## 7. Open gaps (NOT in this package — carry-forward, team_00 prioritization)
- **G2** — precision walk for T2 Services (index+single), T3 Project single, T4 Post single, T5 Blog index (exist as PHP+design; never in the v4 precision pass).
- **G3** — know/code world-page variants + heritage parity check.
- **C2** — sub-document `<img>` overflow on home (26px@375 / 101px@1440; contained, no scrollWidth growth) — localize the node (`.ph`/`.media` cover or manifesto `.mf-bg`) and clamp. Non-blocking.
- Detail: `_COMMUNICATION/team_35/HANDOFF_CLAUDE_CODE_V200_2026-06-03/README.md` §5 + `TEMPLATE_COVERAGE_AUDIT` (referenced; not in this zip — request from team_35 if needed for G2/G3 scoping).

## 8. Risks / notes
- **Permalink flush is the #1 deploy gotcha** — `/projects/` silently 404s without it.
- **Iron Rule #4** — single logical writer: land + deploy this package as one atomic team_100-orchestrated pass; no concurrent theme writers (prior incident).
- B2 (`מֳר`→`מ״ר`) + B3 (home bridges 3-up) + C4 (A/B/C byte-check) = **no live change** — already correct; do not "fix".
- Render against dev only (TLS invalid by design; cert-bypass DEV-ONLY). Production cutover (P005-WP002) is separate.

*team_100 | LOD400 | 2026-06-03 | V200 precision package — characterization + land-spec + external validation*
