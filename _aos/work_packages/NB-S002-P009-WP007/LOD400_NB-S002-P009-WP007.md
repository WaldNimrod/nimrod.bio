# LOD400 — NB-S002-P009-WP007 — Full design implementation (precision walk vs Precision Mockup v5)

**Author:** team_100 (Chief System Architect · Claude Code) · **Date:** 2026-06-03
**Track:** A (STANDARD) · **Milestone:** V200 · **Profile:** L0 spoke (ADR034 R9 — roadmap + git audit; file-based SSoT)
**Baseline:** theme **v0.7.19** @ commit `161e8078` · **Dev:** `https://nimrod-bio-2026.s887.upress.link` · **Local:** `http://localhost:8085` (100% dev mirror)
**Design SSoT (canonical):** `Precision Mockup v5.html` — `_COMMUNICATION/team_35/HANDOFF_v5_2026-06-03/Precision_Mockup_v5.html` (full package, gitignored: `sources/team_35_design_package/HANDOFF_v5_2026-06-03/`)
**Origin:** team_35 full v5 handoff + `DESIGN_COMPLETENESS_REPLY_team_100_2026-06-03_v1` (answers rows 1–5: every page-type now has a v5 precision authority).

---

## §1 · Objective
Bring **every page-type that was NOT in the v4 precision pass** to **v5 pixel-fidelity**, editing **module CSS + template-parts only** (no inline, no overrides layer, `system.css` LOCKED). This completes the V200 design implementation so the whole site matches one consolidated SSoT (v5). The WP006 a11y baseline (axe 0 / Lighthouse a11y ≥95) must hold as a **non-regression** gate throughout.

Already-precision (v4, NOT re-opened here): T7 home · T1 world-soil · T8 about · T8 contact · 404 · search. **Projects archive (G1) is DONE** (WP001: `cpt-project has_archive='projects'`, `/projects/` 200 dev+local) — **verify non-regression only**, do not rebuild.

## §2 · Inputs / SSOT
- **v5 mockup** (screen SSoT): screens `t2`, `t2s`, `t3`, `t4`, `t5`, `heritage`, `t1`+world-switcher (`Precision_Mockup_v5.html`).
- **Build/delta layer:** `04_build_layer/HANDOFF_CLAUDE_CODE_V200/` (theme parts, §06, `G2_G3_C2_PRECISION_2026-06-03_v1.md` — esp. the **G3a +25-line `t1.css` world-accent override** to land here, row 5).
- **Coverage map:** `TEMPLATE_COVERAGE_AUDIT_2026-06-02_v1.md` (screen→template→route).
- **Brand:** `02_brand/system.css` (LOCKED tokens), typography/voice, TAXONOMY-v3.4-LOCKED.
- **a11y baseline:** WP006 (`docs/qa/cdp/v200b/...`, axe_probe.mjs + Lighthouse) — non-regression target.
- **QA harness:** `docs/QA_HARNESS.md` (qa_probe.mjs CDP, Lighthouse via full Chrome, axe_probe.mjs).

## §3 · Scope
**IN (precision walk → v5 fidelity):**
- **G2:** T2 services-index (`archive-service.php`) · T2 service-single (`single-service.php`) · T3 project-single (`single-project.php`) · T4 post-single (`single.php`) · T5 blog-index (`home.php`).
- **G3:** T1 world variants **know/code** (`page-know.php`/`page-code.php` — accent-only) incl. **landing the G3a `t1.css` override** · **heritage** (`page-heritage.php`).

**OUT (not this WP):**
- Projects archive (done WP001 — non-regression check only).
- New copy / content (text is LOCKED per v5 README §2); world-journal content-seeding; `taxonomy-world.php` archive (optional, not for launch).
- Production cutover (P005-WP002); real SFA/tiktrack screenshots (owner-pending); data-layer durability (P005-WP002).
- `system.css` token changes (LOCKED — GCR if ever needed).

## §4 · Per-screen acceptance criteria
> Method per screen: render live (dev) + local at **375 & 1440** via `qa_probe.mjs`/CDP + screenshot; diff against the matching v5 screen; fix drift in **module CSS (`assets/css/t1..t8.css`, components.css) + template-parts** only. "Fidelity" = section order, block structure, type scale, `--sec-y` rhythm, tokens, radii/shadows, RTL logical properties, hover lifts, spark budget (≤3–5 site-wide). Decorative-only deltas are advisory.

### G2 — T2 / T3 / T4 / T5

**AT-1 · T2 Services index (`archive-service.php` → v5 `t2`)**
`.crumbs` → `.page-hero` (eyebrow+num, h1 `clamp(34→60px)`, lede `max 60ch`, `.svc-meta-strip`) → service cards grid `repeat(4,1fr)` gap `clamp(16→22px)` (→1-col ≤520) with world-icon + `.svc-points` + CTA → `.bridges-band` (2-up `.bridges-grid` with `.bridge-card .spine`) → `.final-cta`. Section rhythm `clamp(52→92px)`. No overflow @375/1440.

**AT-2 · T2 Service single (`single-service.php` → v5 `t2s`, NEW)**
`.crumbs` → `.svc-single-hero` (2-col `1fr 1fr` → 1-col ≤1000; `.ssh-tag` world chip, h1, `.ssh-lede`, meta-strip, portrait) → §01 `.feat-grid` `repeat(4,1fr)` of `.feat-tile`(`.ft-k` mono 11.5px/.12em + h4 + p) → `.svc-pull` (centered emphasis) → §02 `.svc-steps` 4× `.svc-step`(`.sn` big serif) → §03 `.projects-row` (3 field-proof projects) → bridge-card (cross-world) → `.final-cta`. **Existing BCS gallery (`t3-gallery`) + meta-strip preserved** (non-regression).

**AT-3 · T3 Project single (`single-project.php` → v5 `t3`)**
`.t3-hero.proj-hero` (`.t3-hero-grid` 2-col; `.t3-hero-tags`, h1 `clamp(28→44px)` `-.015em`, 3-row meta, image + stage-stamp) → `.about-prose` story → `.outcomes` `repeat(4,1fr)` of `.oc-tile`(`.n/.v/.l/.desc`) → `.linked-services` (3 `.ls-card`) → `.proj-gallery` (lead-wider 3-photo) → `.projects-row` related (3) → `.final-cta`. Scope/stage stamps + world chips use the **deep** world tokens (WP006 AA). Galleries (wired data-layer) preserved.

**AT-4 · T4 Post single (`single.php` → v5 `t4`)**
`.article-shell.has-aside` → `.article-head` (meta-top: date·read·worlds·tags, h1, `.a-lede`, author-row) → `.article-hero.ph` → `.post-layout` (`.article-body` measure **~66ch** + `.post-aside` sticky) → body: numbered `h2` (`.num` prefix) `clamp(28→44px)`, `.pull` emphasis, `blockquote`+`.cite`, `.entity-link` (world-colored) → `.article-foot` (author bio + `.related-grid` 3 `.rp-card`). Aside heading-order per WP006 (h3). Sticky aside ≥1000px, stacks below.

**AT-5 · T5 Blog index (`home.php` → v5 `t5`)**
`.page-hero` `.blog-header-grid` (heading + `.stats`) → `.blog-toolbar` (`.blog-filter` tablist all/soil/know/code with `.is-on` + `.view-toggle` grid/flow) → `.posts-grid` `repeat(4,1fr)` `grid-auto-rows:1fr` with `.rp-card.feat` spanning `1/3·1/3` (`.ph` aspect-ratio:auto) + regular `.rp-card` (`.ph` 16/10) → `.blog-end`. Responsive: 2-col ≤1000 (feat full-row), 1-col ≤680. Filter chips functional (existing `t5-filter` JS) — non-regression. **sr-only h2 (WP006) preserved.**

### G3 — world variants + heritage

**AT-6 · T1 know/code accent variants (`page-know.php`/`page-code.php` → v5 `t1` switcher) + LAND G3a**
Land the G3a **`t1.css` world-accent override** (`.t1-world-know`/`.t1-world-code`) — recolours: `.vc-hero-stack`/echo, `.vc-hero .gloss .rule`+`.word`, intro marker, `.lat-anchor` bg + `.lat-side` border, `.anchor-card`/`.anchor-eye`/`.facts .v`, `.post-card:hover h5`, `.s-eyebrow`, and the page-wash gradient — to `--w-know-deep #9a4f2b`/`--w-know #c46a3e`/`--know-wash` and `--w-code-deep #1f5e60`/`--w-code #2d8a8c`/`--code-wash`. **soil unchanged.** Layout identical (accent-only, no fork). **a11y:** recoloured text uses `--w-*-deep` tokens already AA-cleared at WP006 — verify know-deep on `--know-wash` + code-deep on `--code-wash` still ≥4.5.

**AT-7 · Heritage (`page-heritage.php` → v5 `heritage`)**
`.heritage-hero` (`.stamp`, h1, `.h-span`, `.heritage-meta` 4-item grid, `.heritage-cover`) → `.heritage-reading` > `.heritage-body`: **dropcap** first para, **numbered `h2`** (`.num` 01–05), `.pullquote`, `blockquote`+`cite`, `.entity-link`, `<hr>` dividers, prose **~66ch** → `.heritage-end` CTA card. Confirms the WP006 heritage-hero comma-selector fix holds (text on paper, AA). Hero text contrast AA.

## §5 · Global acceptance (every screen)
- **AC-A · a11y non-regression (gate):** `axe_probe.mjs` **0 serious/critical** + **0 color-contrast** on all affected routes; **Lighthouse a11y ≥95** on T2 index+single, T3, T4, T5, world-know, world-code, heritage. (WP006 baseline = 100; must not regress.)
- **AC-B · no overflow:** `qa_probe.mjs` 0 horizontal overflow @375 & @1440 on all affected routes.
- **AC-C · locks:** 0 forbidden terms (Micha; demonstrate-never-name) across HTML+CSS+alt/aria+comments (CDP `--absent` + grep). New copy = v5's approved register only; no TBD/filler.
- **AC-D · discipline:** edits in `assets/css/*.css` + `template-parts/` + the page templates ONLY; **0 inline styles added; no overrides layer; `system.css` byte-identical (git diff empty)**; RTL logical properties only.
- **AC-E · deploy integrity:** `NB_THEME_VERSION` bumped; FTPS deploy; **byte-parity served==repo** on every changed file.
- **AC-F · non-regression of done work:** T7/T1-soil/about/contact/404/search/projects-archive + WP002 mobile + WP006 a11y + wired galleries (project 49/31, service 24) all still pass (qa_probe + spot check).
- **AC-G · validation:** `validate_aos.sh` 0 FAIL.

## §6 · Execution & phasing
Single WP, executed by an implementation session (team_100-orchestrated, Claude). **Recommended phasing** (each phase = deploy + qa_probe + a11y non-regression before next):
- **Phase 1 (G3a + world variants):** land G3a override; verify know/code accents + heritage (smallest, highest-confidence).
- **Phase 2 (G2 singles):** T2 service-single (t2s) + T3 project-single.
- **Phase 3 (G2 indexes/post):** T2 services-index + T4 post + T5 blog-index.
May split into sub-WPs (WP007a/b/c) if scope/sequencing warrants — at the implementation session's discretion with team_100 approval.

## §7 · Super-locks (every changed byte, incl. alt/aria/comments)
1. **Micha / "Micha OS"** — never written.
2. **Demonstrate, never name** — אנטרופיה · נגנטרופיה · רקורסיה · CDIP · cross-domain · פרמקלצר · 3× · אינסטנסים · קואופרטיב · קומון; no marketing clichés. (Note pre-existing non-rendered comment terms `recursion`/`negentropy` are DRIFT_REGISTER A13 cleanup, out of scope — do not introduce new ones.)

## §8 · Validation routing (Iron Rule #1 + #5)
- **L-GATE_SPEC (this LOD400):** cross-engine review by **team_190 (Cursor; Codex unavailable — Cursor ≠ author engine)** → `VERDICT_…_LOD400`.
- **L-GATE_VALIDATE (post-build):** build engine = Claude Code → validate by **team_190 (Cursor) + team_50**. Closure ADR042.

## §9 · Handoff plan (to implementation session)
On L-GATE_SPEC PASS, team_100 issues a HANDOFF to a fresh implementation session containing: this LOD400, the v5 SSoT path, the G3a override location, the per-screen AT list, the WP006 a11y baseline + harness commands, the FTPS procedure, and the clean-baseline pin (`@161e8078`, v0.7.19, local mirror). Build → deploy → qa_probe + a11y non-regression → route to cross-engine validation.

---
*team_100 · LOD400 · NB-S002-P009-WP007 · 2026-06-03 · SSoT = Precision Mockup v5 · baseline v0.7.19 @161e8078 · a11y non-regression gated*
